{
  "name": "request-management-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node src/index.js",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}